{
  "schemaVersion": 1,
  "id": "gametest-quilt-mappings",
  "version": "${version}",
  "environment": "*",
  "mixins": [
    "quilt.gametest.mixins.json"
  ],
  "depends": {
    "minecraft": "*",
    "fabricloader": "${fabric_loader_version}"
  }
}
