{
  "schemaVersion": 1,
  "id": "gametest-quilt-mappings",
  "version": "${version}",
  "environment": "*",
  "entrypoints": {
    "fabric-gametest": [
      "quilt.internal.gametest.TestEntitiesSpawn"
    ]
  },
  "depends": {
    "minecraft": "*",
    "fabricloader": "${fabric_loader_version}",
    "fabric": "${fabric_api_version}"
  }
}
