plugins {
    id 'java'
    alias libs.plugins.quilt.loom
}

dependencies {
    // implementation project(':core')

    minecraft(libs.minecraft)
    mappings(files(project(':core').tasks.tinyJar.archiveFile))
}